# Environment and secrets (never commit)
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Database (optional: uncomment if each dev should have their own DB)
# backend/database.sqlite

# Uploads (optional: uncomment if you don't want to commit uploaded files)
# backend/uploads/

# OS and editor
.DS_Store
Thumbs.db
*.log
